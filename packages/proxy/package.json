{
    "name": "@conshell/proxy",
    "version": "0.1.0",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "npx tsc --build",
        "test": "npx vitest run --passWithNoTests",
        "test:watch": "vitest",
        "clean": "rm -rf dist",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@conshell/core": "workspace:*",
        "@conshell/inference": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^5.6.0",
        "vitest": "^2.1.0"
    }
}
